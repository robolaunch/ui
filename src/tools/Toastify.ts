export const toastifyProperties: any = {
  theme: localStorage.theme === "dark" ? "dark" : "light",
  position: "top-right",
  autoClose: 3000,
  hideProgressBar: false,
  closeOnClick: true,
  pauseOnHover: true,
  draggable: true,
};
